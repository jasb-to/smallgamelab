import Link from "next/link";
import { GAME_CATALOG } from "../../lib/game-catalog";

export default function Games(){return <main className="min-h-screen px-6 py-8 lg:px-10"><header className="mx-auto flex max-w-7xl justify-between"><Link href="/" className="text-sm font-black uppercase tracking-[.18em]">Small Game Lab.</Link><span className="text-xs font-bold uppercase tracking-[.2em] text-zinc-600">The Long Run · Campaign</span></header><section className="mx-auto max-w-7xl py-20"><p className="text-xs font-bold uppercase tracking-[.3em] text-orange-500">The Long Run</p><h1 className="mt-5 max-w-5xl text-6xl font-black tracking-[-.05em] sm:text-8xl">ONE GAME.<br/>FIVE CHAPTERS.</h1><p className="mt-8 max-w-2xl text-sm leading-7 text-zinc-500">Play Mara Vale's complete run from rooftop courier to Meridian Tower. Each chapter changes the mechanic, but Mara, her progression and the story carry forward.</p><Link href="/campaign" className="mt-8 inline-flex bg-orange-500 px-6 py-3 text-sm font-black uppercase text-black">Continue Campaign →</Link><div className="mt-16 border-t border-zinc-800">{GAME_CATALOG.map((c,i)=><article key={c.n} className="group flex flex-col gap-8 border-b border-zinc-800 py-8 md:flex-row md:items-center md:justify-between"><div className="flex gap-6"><span className="pt-2 text-xs text-zinc-600">{c.n}</span><div><h2 className="text-3xl font-black uppercase tracking-tight sm:text-5xl">{c.title}</h2><p className="mt-3 max-w-xl text-sm leading-6 text-zinc-500">{c.desc}</p></div></div><Link href={c.href} className={`w-fit shrink-0 px-6 py-3 text-sm font-black uppercase ${i===0?"bg-orange-500 text-black":"border border-zinc-700 text-zinc-200 hover:border-orange-500"}`}>{c.state} →</Link></article>)}</div></section></main>}
